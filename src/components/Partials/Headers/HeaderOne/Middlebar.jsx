export default function Middlebar({ className, type }) {
  return (
    <div className={`w-full h-[86px] bg-white ${className}`}>
      {/* Middlebar is now empty - logo moved to Navbar */}
    </div>
  );
}
